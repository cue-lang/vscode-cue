# This section of the file will have significant overlap with .gitignore
# with the major difference that .vscodeignore cannot anchor to the root
# of the repo. Hence 'bin/' ignores _all_ directories named 'bin'.
bin/
node_modules/
*.code-workspace
*.vsix

# A static snapshot of what constitues the extension. Exclude this from
# the extension itself.
manifest.txt

# This section is more specific to the extension itself
*.go
*.cue
.eslintrc.json
.vscode-test.mjs
launch.json
.vscode/
*.map
src/test/
esbuild.js

# Legacy files generated as part of skeleton - useful for reference only.
_generated/
